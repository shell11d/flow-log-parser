1. The flow log file and lookup table file are properly formatted CSV files.
2. The program assumes case-insensitive matching for protocol strings 
3. If port/ protocol combination is not found in the lookup table, it is tagged as untagged 
